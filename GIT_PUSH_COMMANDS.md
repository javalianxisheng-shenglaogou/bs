# Git推送命令 - 快速参考

## 📋 准备工作完成情况

### ✅ 已完成
1. ✅ 更新了 `.gitignore` 文件
   - 添加了临时测试文件忽略规则
   - 添加了 `application-dev.yml` 忽略（保护敏感信息）
   
2. ✅ 创建了配置示例文件
   - `.env.example` - 环境变量示例
   - `application-dev.yml.example` - 后端配置示例
   
3. ✅ 更新了 README.md
   - 添加了版本控制功能说明
   - 添加了国际化功能说明
   - 更新了技术栈
   - 更新了文档链接
   - 添加了V1.2.0更新日志
   
4. ✅ 创建了部署文档
   - `DEPLOYMENT.md` - 完整的部署指南
   - `GIT_COMMIT_CHECKLIST.md` - 提交前检查清单
   
5. ✅ 项目文档完善
   - 需求分析文档
   - 技术实现文档
   - 测试指南
   - 问题解决文档

### ⚠️ 需要注意
1. `application-dev.yml` 已添加到 `.gitignore`，不会被提交
2. 临时测试文件已在 `.gitignore` 中配置，不会被提交
3. `node_modules` 和 `target` 目录不会被提交

---

## 🚀 Git操作命令（按顺序执行）

### 步骤1：初始化Git仓库（如果还没有）

```bash
# 检查是否已初始化
git status

# 如果提示 "not a git repository"，则初始化
git init
```

### 步骤2：配置Git用户信息（如果还没有配置）

```bash
git config user.name "Your Name"
git config user.email "your.email@example.com"
```

### 步骤3：查看将要提交的文件

```bash
# 查看当前状态
git status

# 查看将要添加的文件（不实际添加）
git add -n .
```

### 步骤4：添加文件到暂存区

```bash
# 添加所有文件
git add .

# 再次查看状态，确认文件已添加
git status
```

### 步骤5：创建提交

```bash
git commit -m "feat: 初始提交 - 多站点CMS系统V1.2

主要功能：
- 用户管理、站点管理、内容管理
- 工作流审批系统
- 版本控制功能（自动版本、比较、恢复、标记）
- 国际化支持（中英文双语）
- 文件上传、系统日志

技术栈：
- 后端：Spring Boot 2.7.18 + MySQL 8.0 + JWT
- 前端：Vue 3 + TypeScript + Element Plus + Vue I18n
- 数据库迁移：Flyway
- 文档：完整的需求和技术文档

测试状态：
- 所有核心功能测试通过
- 前后端服务正常运行
- API接口测试通过"
```

### 步骤6：添加远程仓库

```bash
# 添加远程仓库
git remote add origin https://gitee.com/jiuxias-da/multi-site-hub.git

# 验证远程仓库
git remote -v
```

### 步骤7：检查分支名称

```bash
# 查看当前分支
git branch

# 如果需要，重命名分支为 main
git branch -M main
```

### 步骤8：推送到远程仓库

#### 选项A：远程仓库为空（首次推送）

```bash
# 推送到 main 分支
git push -u origin main
```

#### 选项B：远程仓库已有内容

```bash
# 先拉取远程内容
git pull origin main --allow-unrelated-histories

# 如果有冲突，解决冲突后
git add .
git commit -m "merge: 合并远程仓库"

# 然后推送
git push -u origin main
```

#### 选项C：强制推送（覆盖远程，谨慎使用）

```bash
# ⚠️ 警告：这会覆盖远程仓库的所有内容
git push -f origin main
```

---

## 📝 完整的命令序列（复制粘贴）

### 场景1：全新仓库（推荐）

```bash
# 1. 初始化（如果需要）
git init

# 2. 添加所有文件
git add .

# 3. 查看状态
git status

# 4. 创建提交
git commit -m "feat: 初始提交 - 多站点CMS系统V1.2

主要功能：
- 用户管理、站点管理、内容管理
- 工作流审批系统
- 版本控制功能（自动版本、比较、恢复、标记）
- 国际化支持（中英文双语）
- 文件上传、系统日志

技术栈：
- 后端：Spring Boot 2.7.18 + MySQL 8.0 + JWT
- 前端：Vue 3 + TypeScript + Element Plus + Vue I18n
- 数据库迁移：Flyway
- 文档：完整的需求和技术文档"

# 5. 添加远程仓库
git remote add origin https://gitee.com/jiuxias-da/multi-site-hub.git

# 6. 重命名分支为 main
git branch -M main

# 7. 推送到远程
git push -u origin main
```

### 场景2：远程仓库已有内容

```bash
# 1. 添加所有文件
git add .

# 2. 创建提交
git commit -m "feat: 初始提交 - 多站点CMS系统V1.2"

# 3. 添加远程仓库（如果还没有）
git remote add origin https://gitee.com/jiuxias-da/multi-site-hub.git

# 4. 拉取远程内容
git pull origin main --allow-unrelated-histories

# 5. 推送到远程
git push -u origin main
```

---

## 🔍 验证推送结果

### 1. 在终端验证

```bash
# 查看远程仓库信息
git remote -v

# 查看提交历史
git log --oneline

# 查看远程分支
git branch -r
```

### 2. 在浏览器验证

打开 Gitee 仓库页面：
```
https://gitee.com/jiuxias-da/multi-site-hub
```

检查以下内容：
- ✅ README.md 正确显示
- ✅ 源代码目录完整（backend/src, frontend/src）
- ✅ 文档目录完整（docs/）
- ✅ 配置文件存在（pom.xml, package.json）
- ❌ node_modules 未被上传
- ❌ target 未被上传
- ❌ 临时测试文件未被上传

---

## ⚠️ 常见问题解决

### 问题1：推送失败 - 认证错误

```bash
# 错误信息：
# fatal: Authentication failed

# 解决方案：
# 1. 检查用户名和密码
# 2. 或使用SSH密钥
```

### 问题2：推送失败 - 远程有更新

```bash
# 错误信息：
# ! [rejected] main -> main (fetch first)

# 解决方案：
git pull origin main --allow-unrelated-histories
# 解决冲突后
git push origin main
```

### 问题3：文件太大

```bash
# 错误信息：
# remote: error: File xxx is 123.45 MB; this exceeds GitHub's file size limit of 100.00 MB

# 解决方案：
# 1. 检查大文件
find . -type f -size +50M

# 2. 移除大文件
git rm --cached path/to/large/file

# 3. 添加到 .gitignore
echo "path/to/large/file" >> .gitignore

# 4. 重新提交
git add .
git commit --amend
git push -f origin main
```

### 问题4：想要修改提交信息

```bash
# 修改最后一次提交
git commit --amend

# 如果已推送，需要强制推送
git push -f origin main
```

### 问题5：想要删除某个文件

```bash
# 从Git中删除，但保留本地文件
git rm --cached path/to/file

# 添加到 .gitignore
echo "path/to/file" >> .gitignore

# 提交更改
git add .
git commit -m "chore: 移除敏感文件"
git push origin main
```

---

## 📊 推送后的文件统计

### 应该被提交的文件
- ✅ 源代码文件（.java, .ts, .vue, .tsx）
- ✅ 配置文件（.yml, .xml, .json, .ts）
- ✅ 文档文件（.md, .txt）
- ✅ 数据库脚本（.sql）
- ✅ 静态资源（.html, .css）

### 不应该被提交的文件
- ❌ 依赖包（node_modules/, target/）
- ❌ 构建产物（dist/, build/）
- ❌ 日志文件（*.log, logs/）
- ❌ 上传文件（uploads/）
- ❌ 临时文件（*.tmp, *.temp）
- ❌ 测试文件（test_*.ps1, test*.html）
- ❌ 敏感配置（application-dev.yml）

---

## 🎯 下一步

推送成功后：

1. **在Gitee上配置项目**
   - 添加项目描述
   - 设置项目标签
   - 配置仓库可见性

2. **添加项目成员**（如果需要）
   - 邀请协作者
   - 设置权限

3. **配置CI/CD**（可选）
   - Gitee Go
   - Jenkins
   - GitHub Actions

4. **添加项目徽章**（可选）
   - 构建状态
   - 代码覆盖率
   - 许可证

---

**准备好了吗？开始推送吧！** 🚀

```bash
# 一键执行（全新仓库）
git init && \
git add . && \
git commit -m "feat: 初始提交 - 多站点CMS系统V1.2" && \
git remote add origin https://gitee.com/jiuxias-da/multi-site-hub.git && \
git branch -M main && \
git push -u origin main
```

